# Everything needed to lint, type-check and test the project.
#   pip install -r requirements-dev.txt
#   ruff check . && mypy "Phase 5" && pytest
pytest==8.4.2
pytest-cov==6.2.1
ruff==0.14.2
mypy==1.18.2
